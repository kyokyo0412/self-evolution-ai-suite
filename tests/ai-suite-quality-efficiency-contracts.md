# AI Suite Quality & Efficiency Contracts

## TDD Team Process Contract
- The `tdd-team` markdown file must include the phrase "Maximize parallel tool calls".
- The `tdd-team` markdown file must include references to "linter checks" or "ReadLints" explicitly.

## AI Expert Skill Contract
- The `ai-expert` markdown file must contain instructions to embed "parallel tool calls" into optimized prompts.
- The `ai-expert` markdown file must contain instructions to embed "quality checks" or "linter checks" into optimized prompts.

# Evolution Report: local-cursor-collection

**Host:** local
**Timestamp (UTC):** 20260604T061500Z
**Local suite:** .ai-suite

## Task Summary
Collected two significant evolutions that were manually applied directly to the global Cursor skills directory (`~/.cursor/skills/`) back into the local `.ai-suite` project repository. 

## Improvement Target
- **File:** `domains/vmware/skills/bugzilla-debug.md`
- **File:** `domains/vmware/scripts/bugzilla-rest-api/attachment_downloader.py`
- **Tier:** domains/vmware

## Root Cause
- **Category:** Instruction Completeness
- **Severity:** High
- **Description:** The `wget` command provided in the previous evolution failed to download deeply nested support bundles because `wget` has a default recursion depth limit of 5 (`-l 5`). Furthermore, the `attachment_downloader.py` script was originally read-only for the REST API and forced the AI to rely on external bash commands to download bundles.

## Change Description
1. **bugzilla-debug.md:** Updated the manual `wget` fallback instruction to include `-l inf` (infinite recursion depth) to bypass the default 5-level limit, ensuring the AI correctly downloads deeply nested VMware logs.
2. **attachment_downloader.py:** Massively evolved the python script. It now includes logic to authenticate and scan bug comments and descriptions via REST API to automatically extract explicit `engweb` and `nsxqe-logs` URLs. It also introduces a `download_external_url` function that uses Python's `subprocess` to execute the `-l inf` `wget` command automatically, fully automating support bundle downloads within the python script itself.

## Generality Gate Result
- Q1 (all agents): NO
- Q2 (task process procedure): NO
- Q3 (agent-specific): NO
- Q4 (domain-specific): YES
- **Tier chosen:** domains/vmware
- **Justification:** Both the `bugzilla-debug` instructions and the `attachment_downloader.py` script are heavily customized for VMware-specific logging infrastructure (`engweb` and `nsxqe-logs`).

## Delta Summary
- Added `-l inf` to the `wget` instructions in `bugzilla-debug.md`.
- Added `get_comments()`, `get_bug_details()`, `find_bundle_urls()`, and `download_external_url()` to `attachment_downloader.py` to make the script a fully automated downloader that no longer relies on the AI constructing manual bash scripts.
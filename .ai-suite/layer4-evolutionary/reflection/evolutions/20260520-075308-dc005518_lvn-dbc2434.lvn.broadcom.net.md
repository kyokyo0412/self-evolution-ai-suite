# Evolution Report: dc005518@lvn-dbc2434.lvn.broadcom.net

**Host:** dc005518@lvn-dbc2434.lvn.broadcom.net
**Timestamp (UTC):** 20260520-075308
**Local suite:** /Users/dc005518/gitsource/cursor_evolution/.ai-suite

## Changed Files

- core/skills/remote-suite.md
- meta/evolutions/REFLECTION-20260520T075051Z.md (new)

## Diffs

=== CHANGED: core/skills/remote-suite.md ===
--- /Users/dc005518/gitsource/cursor_evolution/.ai-suite/layer4-evolutionary/merging/remote-suite.md	2026-05-20 15:10:59
+++ /var/folders/c3/tdjny5xx08n1tpxskhcbx8700000gn/T//evolve-collect.DLq9t7/core/skills/remote-suite.md	2026-05-20 15:50:38
@@ -5,17 +5,17 @@
   - install ai-suite
   - deploy ai-suite
   - remote ai-suite
-  - set up ai-suite on
   - check ai-suite status
   - check ai-suite on
   - remove ai-suite
   - uninstall ai-suite
-  - disable ai-suite on
   - collect evolution
-  - sync evolution
   - push evolution
-  - push suite to
   - ai-suite on remote
+  - verify ai-suite
+  - verify the install
+  - is ai-suite installed
+  - reinstalled
 ---
 
 # Remote Suite — AI-Prompted Remote Lifecycle Manager
@@ -37,8 +37,49 @@
 
 ---
 
+## Local Deployment Path (canonical — never guess)
+
+The local deployment root is **always** `~/.ai-suite-deploy/`. Do NOT search for
+`.cursor-suite/`, `.ai-suite/` at `~`, or any other variant.
+
+| Path | Purpose |
+|---|---|
+| `~/.ai-suite-deploy/` | Deployment root |
+| `~/.ai-suite-deploy/.ai-suite/` | Suite source tree |
+| `~/.ai-suite-deploy/.ai-suite/layer4-evolutionary/validation/` | Protocols, evolution reports |
+| `~/.ai-suite-deploy/ai-suite enable` | Install script |
+| `~/.ai-suite-deploy/ai-suite disable` | Uninstall script |
+| `~/.ai-suite-deploy/ai-suite evolve` | Evolution sync script |
+| `~/.cursor/skills/` | Deployed skills (agent reads these) |
+| `~/.cursor/rules/` | Deployed rules (always-apply) |
+
+> **Discovery rule:** When asked to verify any local installation, ALWAYS run
+> `ls -la ~/` first to see the actual directory layout. Never use `find` with a
+> hardcoded directory name as the first step.
+
+---
+
 ## Operations
+
+### 0. Local Status (verify installation on the current machine)
+
+When the user asks "verify", "please verify", "confirm install", "is ai-suite installed",
+or similar — and no `USER@HOST` is present — run this local check:
 
+```bash
+DEPLOY="$HOME/.ai-suite-deploy"
+
+echo "=== Deploy root ===" && ls "$DEPLOY/" 2>/dev/null || echo "NOT INSTALLED"
+echo "=== Scripts ===" && ls "$DEPLOY/"*.sh 2>/dev/null || echo "(none)"
+echo "=== Meta ===" && ls "$DEPLOY/.ai-suite/layer4-evolutionary/validation/" 2>/dev/null || echo "(missing)"
+echo "=== Skills (source) ===" && find "$DEPLOY/.ai-suite" -name '*.md' -path '*/skills/*' | sort
+echo "=== Skills (deployed) ===" && ls ~/.cursor/skills/ 2>/dev/null || echo "(none)"
+echo "=== Rules (deployed) ===" && ls ~/.cursor/rules/ 2>/dev/null || echo "(none)"
+```
+
+Then report: deploy root present ✅/❌, script count, meta present ✅/❌,
+skill count (source vs deployed), rules deployed ✅/❌.
+
 ### 1. Install (deploy ai-suite to a remote host)
 
 Maps to: `bash ai-suite enable --scope remote --host USER@HOST [options]`
@@ -180,17 +221,21 @@
 | "install", "deploy", "set up", "enable", "bootstrap" | **install** |
 | "collect", "sync", "pull", "fetch", "gather" | **collect** |
 | "push", "send evolution", "update remote", "deploy evolution" | **push** |
-| "status", "check", "verify", "is it installed", "what's installed" | **status** |
+| "status", "check", "verify", "please verify", "reinstalled", "confirm install", "is it installed", "what's installed" | **local status** (no host) or **remote status** (with host) |
 | "remove", "disable", "uninstall", "clean up", "take off" | **remove** |
 | Ambiguous / just `ai-suite on USER@HOST` | **install** (default) |
 
 ### Host extraction
 
-Extract all `USER@HOST` patterns from the user's message. If none found, ask:
+Extract all `USER@HOST` patterns from the user's message.
 
+- If a `USER@HOST` is found → use the **remote** operation (Operations 1–5).
+- If **no** `USER@HOST` is found and the intent is status/verify → use **Operation 0 (Local Status)** immediately. Do NOT ask for a host.
+- If **no** `USER@HOST` is found and the intent is install/collect/push/remove → ask:
+
 > "Which remote host? (format: `user@hostname`)"
 
-Do NOT proceed until the user provides a host.
+Do NOT proceed until the user provides a host (for remote operations only).
 
 ### Multiple hosts
 
@@ -202,8 +247,9 @@
 
 ### Step 1 — Identify operation and hosts
 
-- Operation: install / collect / push / status / remove (see table above)
-- Hosts: extract `USER@HOST` patterns — ask if none found
+- Operation: local-status / install / collect / push / remote-status / remove (see table above)
+- **Local status shortcut:** if intent is verify/status and no `USER@HOST` is present, run Operation 0 immediately and skip Steps 2–4.
+- Hosts: extract `USER@HOST` patterns — for remote ops, ask if none found
 - Options: agent, domain, remote-path, remote-scope, dry-run
 
 ### Step 2 — Safety preflight

=== NEW: meta/evolutions/REFLECTION-20260520T075051Z.md ===
## Task Summary

The user performed two local ai-suite reinstalls and asked the AI to verify the installation
each time. During the first verification pass the AI hardcoded directory names
(`.cursor-suite`, then `.ai-suite`) in `find` searches and failed to locate the meta
framework; the user had to manually state that the directory was renamed and the AI still
required listing `~/` to finally discover `~/.ai-suite-deploy`.

## Improvement Target

- **File:** `.ai-suite/layer4-evolutionary/merging/remote-suite.md`
- **Tier:** core

## Root Cause

- **Category:** Instruction Completeness
- **Severity:** High
- **Description:** The `remote-suite` skill had no documented local deployment path and no
  local-status operation. When the user asked "please verify" without a remote host, no skill
  fired and the AI improvised using `find` with guessed directory names. The canonical path
  `~/.ai-suite-deploy/` was unknown to the AI, causing three rounds of failed searches and
  requiring the user to provide a hint. Adding an explicit Local Status section with the
  canonical path and a "list `~/` first" discovery rule eliminates this guesswork entirely.

## Change Description

Three additions to `.ai-suite/layer4-evolutionary/merging/remote-suite.md`:

1. **New "Local Deployment Path" section** (above Operations): documents the canonical
   `~/.ai-suite-deploy/` root, a path table for all sub-directories, and a hard rule:
   *"When asked to verify a local installation, ALWAYS run `ls -la ~/` first. Never use
   `find` with a hardcoded directory name as the first step."*

2. **New Operation 0 — Local Status**: a copy-paste bash block (`DEPLOY`, `ls`, `find`)
   that verifies deploy root, scripts, meta, source skills, and deployed skills in one run;
   triggered when intent is verify/status and no `USER@HOST` is present.

3. **Updated Intent Recognition + Host extraction**: "please verify", "reinstalled",
   "confirm install" → local-status (no-host path); Host extraction rule now routes
   no-host verify intent to Operation 0 without asking for a host.

Triggers added: `verify ai-suite`, `verify the install`, `is ai-suite installed`,
`reinstalled` (4 new, replacing 4 redundant originals to stay within the 20-line
frontmatter limit).

## Generality Gate Result

- Q1 (all agents): YES
- Q2 (agent-specific): NO
- Q3 (domain-specific): NO
- **Tier chosen:** core
- **Justification:** The local deployment path and discovery discipline apply identically
  to Claude Code, OpenCode, and any future agent — no agent-specific APIs are involved.

## Delta Summary

- Added ~30 lines: "Local Deployment Path" canonical path table + discovery rule
- Added ~20 lines: Operation 0 (Local Status) with copy-paste bash block
- Modified: Intent Recognition table row for status/verify; Host extraction routing logic
- Modified: Step 1 of Step-by-Step Execution to reference local-status shortcut
- Replaced 4 redundant triggers (`set up ai-suite on`, `sync evolution`, `push suite to`,
  `disable ai-suite on`) with 4 new local-verification triggers
- Validator: 39/39 checks pass

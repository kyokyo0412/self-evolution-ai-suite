# Evolution Report: local-cursor-scripts

**Host:** local
**Timestamp (UTC):** 20260604T035000Z
**Local suite:** .ai-suite

## Task Summary
Collected the evolved Python scripts (including `attachment_downloader.py`) from the local global cursor environment (`~/.cursor/skills/bugzilla-rest-api/scripts/`) back into the `.ai-suite` repository so they can be version-controlled alongside the markdown skills.

## Improvement Target
- **File:** `.ai-suite/layer3-registry/domains/vmware/scripts/bugzilla-rest-api/attachment_downloader.py`
- **Tier:** domains/vmware

## Root Cause
- **Category:** Instruction Completeness
- **Severity:** High
- **Description:** While the Markdown file (`bugzilla-rest-api.md`) was updated to advise the user to use `wget` for recursive directory downloads from `engweb`, the underlying Python script (`attachment_downloader.py`) was also evolved in the local Cursor environment to print this exact hint with the pre-computed `wget` URL when users list attachments. However, this script evolution was stranded in `~/.cursor/skills/` and was not tracked in the main repository.

## Change Description
Copied the entire `bugzilla-rest-api/scripts` directory from `~/.cursor/skills/bugzilla-rest-api/scripts/` into `.ai-suite/layer3-registry/domains/vmware/scripts/bugzilla-rest-api/`. The `attachment_downloader.py` script now dynamically zero-pads the Bug ID to 8 digits, constructs the slashes (e.g. `0/3/7/1/3/7/4/4`), and prints the `wget` helper command to the terminal whenever `python attachment_downloader.py <id> list` is run.

## Generality Gate Result
- Q1 (all agents): NO
- Q2 (task process procedure): NO
- Q3 (agent-specific): NO
- Q4 (domain-specific): YES
- **Tier chosen:** domains/vmware
- **Justification:** The `attachment_downloader.py` script contains hardcoded references to VMware's `engweb` and Bugzilla REST API endpoints.

## Delta Summary
- Brought `attachment_downloader.py`, `bug_search.py`, and `auth_helper.py` into the git repository under `.ai-suite/layer3-registry/domains/vmware/scripts/bugzilla-rest-api/`.
- `attachment_downloader.py` now includes the `engweb` padded string calculator and `wget` tip logic.
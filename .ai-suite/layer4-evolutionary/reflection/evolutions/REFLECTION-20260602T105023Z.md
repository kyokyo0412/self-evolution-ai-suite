## Task Summary
The human requested to "push the evolution to local cursor global". The agent inferred that this meant reinstalling globally but didn't have an explicit trigger to lean on inside the `local-suite.md` skill, leaving the reasoning unmapped and manual.

## Improvement Target
- **File:** `.ai-suite/layer3-registry/core/local-suite.md`
- **Tier:** core

## Root Cause
- **Category:** Trigger Accuracy
- **Severity:** High
- **Description:** The `local-suite.md` skill lacked the explicit `push evolution` or `update` triggers. While `remote-suite.md` explicitly supports pushing evolutions, the local equivalent is simply re-running `ai-suite enable`. Because this wasn't documented, the agent had to manually parse the intent outside of a defined skill structure.

## Change Description
Added `push evolution`, `push the evolution`, and `update ai suite` to the `triggers` list in `local-suite.md`. Also added an explicit note explaining that pushing/updating locally maps safely to the Install operation (running `ai-suite enable`), which overwrites the deployed files with the evolved ones.

## Generality Gate Result
- Q1 (all agents): YES
- Q2 (agent-specific): NO
- Q3 (domain-specific): NO
- **Tier chosen:** core
- **Justification:** Pushing/updating the suite locally applies equally to all supported agents (Cursor, Claude, etc.).

## Delta Summary
- Added `push evolution` and `update` variants to `triggers`.
- Updated the "Install" header to "Install / Update".
- Added an explanatory `**Note on updating/pushing:**` in the markdown body mapping it to `ai-suite enable`.

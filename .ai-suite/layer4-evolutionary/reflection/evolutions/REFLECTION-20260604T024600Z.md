# Evolution Report: local-cursor

**Host:** local
**Timestamp (UTC):** 20260604T024600Z
**Local suite:** .ai-suite

## Task Summary
Collected a manual, out-of-band evolution made directly in the global Cursor skills directory (`~/.cursor/skills/bugzilla-debug/SKILL.md`) back into the local `.ai-suite` project repository. The evolution addressed the inability of the Bugzilla REST API to download full directory attachments (e.g., large support bundles).

## Improvement Target
- **File:** `domains/vmware/skills/bugzilla-debug.md`
- **Tier:** domains/vmware

## Root Cause
- **Category:** Instruction Completeness
- **Severity:** High
- **Description:** The `bugzilla-debug` skill previously instructed the AI to download support bundles via the Bugzilla REST API. However, the REST API only supports single files, and fails when the attachment is a directory structure (which is common for VMware log bundles stored on `engweb`). This caused the AI to fail at the "Environment Setup" phase.

## Change Description
Updated the Environment Setup phase to explicitly provide the workaround. The skill now provides precise instructions on how to use `wget` to recursively download the support bundle directory from the `engweb` static server by padding the Bug ID and converting it to a directory path.

## Generality Gate Result
- Q1 (all agents): NO
- Q2 (task process procedure): NO
- Q3 (agent-specific): NO
- Q4 (domain-specific): YES
- **Tier chosen:** domains/vmware
- **Justification:** Bugzilla and `engweb` support bundles are specific to the VMware domain logic.

## Delta Summary
- Added **CRITICAL ATTACHMENT DOWNLOAD INSTRUCTIONS** to Step 2.
- Provided explicit `wget` command and logic for parsing a bug ID into the `engweb` URL format.
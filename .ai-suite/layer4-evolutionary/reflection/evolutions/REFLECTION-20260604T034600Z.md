# Evolution Report: local-cursor

**Host:** local
**Timestamp (UTC):** 20260604T034600Z
**Local suite:** .ai-suite

## Task Summary
Collected a manual, out-of-band evolution made directly in the global Cursor skills directory (`~/.cursor/skills/bugzilla-rest-api/SKILL.md`) back into the local `.ai-suite` project repository. 

## Improvement Target
- **File:** `domains/vmware/skills/bugzilla-rest-api.md`
- **Tier:** domains/vmware

## Root Cause
- **Category:** Instruction Completeness
- **Severity:** High
- **Description:** The `bugzilla-rest-api` skill previously referenced hardcoded scripts located in a legacy external repository (`~/gitsource/deva/deva/skills/infra/bugzilla-rest-api/scripts/`). It also instructed the AI to use the REST API to download attachments, which fails for directories (e.g. VMware support bundles). 

## Change Description
Updated the script paths to point directly to the deployed cursor skills location (`~/.cursor/skills/bugzilla-rest-api/scripts/`). Added an explicit note forbidding the AI from using the Python script to download attachments, instructing it to use `wget` to `engweb` recursively using a zero-padded, slashed Bug ID format instead.

## Generality Gate Result
- Q1 (all agents): NO
- Q2 (task process procedure): NO
- Q3 (agent-specific): NO
- Q4 (domain-specific): YES
- **Tier chosen:** domains/vmware
- **Justification:** Bugzilla and `engweb` support bundles are specific to the VMware domain logic, and the Python scripts are VMware-specific utility scripts.

## Delta Summary
- Replaced hardcoded legacy `~/gitsource/deva/...` script paths with dynamic `~/.cursor/skills/bugzilla-rest-api/scripts/...` paths.
- Replaced the attachment download instruction with a `wget` fallback targeting the `engweb` HTTP server using zero-padded Bug IDs.
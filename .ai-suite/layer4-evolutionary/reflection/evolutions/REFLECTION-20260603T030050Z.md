## Task Summary
The human requested improvements to the ai suite self-evolution functionality, specifically the creation of a publishing package, evolution categorization, and merging evolutions during collection/push.

## Improvement Target
- **File:** `.ai-suite/layer1-abstraction/agents/cursor/skills/ai-suite-architect.md`
- **Tier:** agents/cursor

## Root Cause
- **Category:** Instruction Completeness
- **Severity:** High
- **Description:** When the AI (acting as `tdd-team` or architect) added new directory tiers (e.g., `core/process` or `domains`), it did not update the hardcoded paths in the infrastructure scripts (`meta/validate-suite.sh`, `meta/run-acceptance-tests.sh`, and `agents/*/adapter.sh`). This caused the AI to loop on failures in the acceptance tests.

## Change Description
Added a strict instruction to the `ai-suite-architect` skill's "Rules of Engagement" to ensure tier registrations are updated across infrastructure scripts whenever a new directory tier or category is created.

## Generality Gate Result
- Q1 (all agents): NO (Architect is Cursor-specific)
- Q2 (task process): NO
- Q3 (agent-specific): YES
- Q4 (domain-specific): NO
- **Tier chosen:** agents/cursor
- **Justification:** The `ai-suite-architect.md` skill is specific to generating Cursor artifacts and resides in the Cursor agent tier.

## Delta Summary
- Added rule: `- **Tier Registration.** When creating a new artifact category or directory tier (e.g., \`core/process\`, \`domains\`), you MUST explicitly update the infrastructure scripts (\`meta/validate-suite.sh\`, \`meta/run-acceptance-tests.sh\`, and \`agents/*/adapter.sh\`) to scan the new path.`

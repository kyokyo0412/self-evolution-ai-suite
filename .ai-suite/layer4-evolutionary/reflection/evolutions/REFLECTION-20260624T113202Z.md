## Task Summary
The human requested to run reflection on the previous task, which involved the `tdd-team` executing a refactoring to merge two skills. During the task, the agent executed step 3.4 (REFACTOR) but did not explicitly clean up or identify the old obsolete files naturally without prompting. The goal is to enforce that refactoring explicitly includes cleanup of redundant, deprecated, or replaced artifacts during Phase 3.

## Improvement Target
- **File:** `.ai-suite/layer3-registry/core/tdd-team.md`
- **Tier:** core/process

## Root Cause
- **Category:** Instruction Completeness
- **Severity:** Medium
- **Description:** The `tdd-team` skill's Phase 3.4 was labeled "REFACTOR" and constrained only to SOLID / DRY improvements. It did not instruct the Developer to actively seek out and delete obsolete files or logic replaced by the new implementation, leading to leftover files (e.g. `prompt-compiler.md` legacy test files).

## Change Description
Updated Phase 3.4 of `.ai-suite/layer3-registry/core/tdd-team.md` to be "REFACTOR & CLEANUP". Added an explicit instruction: "Actively identify and delete deprecated files, redundant logic, or replaced features."

## Generality Gate Result
- Q1 (all agents): YES
- Q2 (agent-specific): YES (process)
- Q3 (domain-specific): NO
- **Tier chosen:** core/process
- **Justification:** The `tdd-team` is a universal stage-gated process that applies to all agents operating in the AI suite.

## Delta Summary
- Changed phase heading from `3.4 REFACTOR` to `3.4 REFACTOR & CLEANUP`
- Added explicit instruction: `Actively identify and delete deprecated files, redundant logic, or replaced features.`
